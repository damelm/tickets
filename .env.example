# Postgres (usado por docker-compose y por server/src/db/pool.js)
POSTGRES_USER=changeme
POSTGRES_PASSWORD=changeme
POSTGRES_DB=tickets
POSTGRES_PORT=5432
DATABASE_URL=postgres://changeme:changeme@localhost:5432/tickets

# Server
PORT=3001
JWT_SECRET=changeme
JWT_EXPIRES_IN=2h
CLIENT_ORIGIN=http://localhost:5173
PGPOOL_MAX=20
GOOGLE_CLIENT_ID=changeme
